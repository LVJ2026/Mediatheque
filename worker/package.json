{
  "name": "mediatheque-worker",
  "private": true,
  "scripts": { "dev": "wrangler pages dev public", "deploy": "wrangler pages deploy public --project-name mediatheque-reservations" },
  "devDependencies": { "wrangler": "^3.99.0" }
}
